import { parseEventLogs as a } from "./index165.js";
import { formatLog as i } from "./index142.js";
async function m(e, { filter: t }) {
  const n = "strict" in t && t.strict, o = await t.request({
    method: "eth_getFilterChanges",
    params: [t.id]
  });
  if (typeof o[0] == "string")
    return o;
  const s = o.map((r) => i(r));
  return !("abi" in t) || !t.abi ? s : a({
    abi: t.abi,
    logs: s,
    strict: n
  });
}
export {
  m as getFilterChanges
};
