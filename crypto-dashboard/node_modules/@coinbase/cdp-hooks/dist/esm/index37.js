let i = 256, t;
function s(e = 11) {
  if (!t || i + e > 256 * 2) {
    t = "", i = 0;
    for (let r = 0; r < 256; r++)
      t += (256 + Math.random() * 256 | 0).toString(16).substring(1);
  }
  return t.substring(i, i++ + e);
}
export {
  s as uid
};
