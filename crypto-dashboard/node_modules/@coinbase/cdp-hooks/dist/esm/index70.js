import { parseEventLogs as a } from "./index165.js";
import { formatLog as e } from "./index142.js";
async function c(g, { filter: t }) {
  const s = t.strict ?? !1, o = (await t.request({
    method: "eth_getFilterLogs",
    params: [t.id]
  })).map((r) => e(r));
  return t.abi ? a({
    abi: t.abi,
    logs: o,
    strict: s
  }) : o;
}
export {
  c as getFilterLogs
};
