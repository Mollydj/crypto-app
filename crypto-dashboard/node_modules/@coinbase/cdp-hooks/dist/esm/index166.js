import { InvalidAddressError as e } from "./index129.js";
import { isAddress as o } from "./index156.js";
function i(r, s) {
  if (!o(r, { strict: !1 }))
    throw new e({ address: r });
  if (!o(s, { strict: !1 }))
    throw new e({ address: s });
  return r.toLowerCase() === s.toLowerCase();
}
export {
  i as isAddressEqual
};
