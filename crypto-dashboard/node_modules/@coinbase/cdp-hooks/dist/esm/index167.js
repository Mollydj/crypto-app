import { SizeOverflowError as z, InvalidHexBooleanError as m } from "./index123.js";
import { size as f } from "./index106.js";
import { trim as t } from "./index147.js";
function u(i, { size: e }) {
  if (f(i) > e)
    throw new z({
      givenSize: f(i),
      maxSize: e
    });
}
function a(i, e = {}) {
  const { signed: n } = e;
  e.size && u(i, { size: e.size });
  const r = BigInt(i);
  if (!n)
    return r;
  const o = (i.length - 2) / 2, s = (1n << BigInt(o) * 8n - 1n) - 1n;
  return r <= s ? r : r - BigInt(`0x${"f".padStart(o * 2, "f")}`) - 1n;
}
function g(i, e = {}) {
  let n = i;
  if (e.size && (u(n, { size: e.size }), n = t(n)), t(n) === "0x00")
    return !1;
  if (t(n) === "0x01")
    return !0;
  throw new m(n);
}
function w(i, e = {}) {
  return Number(a(i, e));
}
export {
  u as assertSize,
  a as hexToBigInt,
  g as hexToBool,
  w as hexToNumber
};
