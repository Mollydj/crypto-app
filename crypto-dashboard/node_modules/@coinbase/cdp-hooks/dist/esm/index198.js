import { ChainDoesNotSupportContract as e } from "./index122.js";
function a({ blockNumber: o, chain: r, contract: c }) {
  const t = r?.contracts?.[c];
  if (!t)
    throw new e({
      chain: r,
      contract: { name: c }
    });
  if (o && t.blockCreated && t.blockCreated > o)
    throw new e({
      blockNumber: o,
      chain: r,
      contract: {
        name: c,
        blockCreated: t.blockCreated
      }
    });
  return t.address;
}
export {
  a as getChainContractAddress
};
