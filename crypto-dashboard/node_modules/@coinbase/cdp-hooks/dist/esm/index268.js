import { BaseError as u } from "./index107.js";
function a(r) {
  let l = !0, i = "", f = 0, t = "", o = !1;
  for (let n = 0; n < r.length; n++) {
    const e = r[n];
    if (["(", ")", ","].includes(e) && (l = !0), e === "(" && f++, e === ")" && f--, !!l) {
      if (f === 0) {
        if (e === " " && ["event", "function", ""].includes(t))
          t = "";
        else if (t += e, e === ")") {
          o = !0;
          break;
        }
        continue;
      }
      if (e === " ") {
        r[n - 1] !== "," && i !== "," && i !== ",(" && (i = "", l = !1);
        continue;
      }
      t += e, i += e;
    }
  }
  if (!o)
    throw new u("Unable to normalize signature.");
  return t;
}
export {
  a as normalizeSignature
};
