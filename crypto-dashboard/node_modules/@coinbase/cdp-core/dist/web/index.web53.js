import { ValidationError as n, InternalError as o } from "./index.web32.js";
function i(r, t) {
  if (!r)
    throw new n("User must be signed in");
  if (!t)
    throw new o("No auth state found");
  return { user: r, authState: t };
}
export {
  i as validateUserAuthState
};
