import { fromNumber as r } from "./index.native174.js";
import { validateUserConnected as a } from "./index.native35.js";
async function s(t, e) {
  const n = await a();
  return t.emit("connect", {
    chainId: r(e.getState().chainId)
  }), n.evmSmartAccountObjects?.map((c) => c.address) || [];
}
export {
  s as handleRequestAccounts
};
