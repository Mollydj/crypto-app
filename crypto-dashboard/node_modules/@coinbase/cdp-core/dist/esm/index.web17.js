import { initiateAuthentication as n } from "@coinbase/cdp-api-client";
import { Analytics as t } from "./index.web2.js";
import "./index.web3.js";
import { getCoreAuthManager as o, getConfig as a } from "./index.web49.js";
async function c(e) {
  if (t.sendActionCallEvent({ name: "link_email" }), !await o().isSignedIn())
    throw new Error("User must be signed in to link an email");
  const i = await n(a().projectId, {
    email: e,
    type: "email"
  });
  return {
    flowId: i.flowId,
    message: "message" in i ? i.message : ""
  };
}
export {
  c as linkEmail
};
