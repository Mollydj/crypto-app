import { toHex as a } from "viem";
import { userOperationChainIdToNameMapping as o } from "./index.native47.js";
import { validateUserConnected as i } from "./index.native35.js";
import "zustand";
import "@coinbase/cdp-api-client";
async function h() {
  return !(await i()).evmSmartAccounts?.length ? {} : Object.keys(
    o
  ).reduce((t, e) => {
    const r = a(Number(e));
    return t[r] = {
      atomic: { status: "supported" },
      paymasterService: { supported: !0 },
      dataSuffix: { supported: !0 }
    }, t;
  }, {});
}
export {
  h as handleGetCapabilities
};
