import { Children as l, isValidElement as t } from "react";
const a = (r) => typeof r == "object" && r !== null && "children" in r && (t(r.children) || Array.isArray(r.children)), o = 10, s = (r, n, i = 0) => i >= o ? !1 : l.toArray(r).some((e) => {
  if (t(e)) {
    if (e.type === n)
      return !0;
    if (a(e.props))
      return s(e.props.children, n, i + 1);
  }
  return !1;
});
export {
  s as childrenHasComponent
};
