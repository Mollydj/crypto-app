const s = (r, n) => {
  if (!r?.contentWindow || !r?.src) return;
  let e = "";
  try {
    e = new URL(r.src).origin;
  } catch (o) {
    console.error("Error parsing iframe src", o);
  }
  e && r.contentWindow?.postMessage(n, e);
};
export {
  s as sendIframeMessage
};
